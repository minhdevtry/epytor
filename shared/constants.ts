// ─── Constants shared across files ──────────────────────────────────────────
// Importable from both the extension side (Node) and the WebView side (Browser)

/** Default height (px) of the editor top toolbar; matches the Crepe topbar default */
export const DEFAULT_TOPBAR_HEIGHT = 40;

/** Viewport edge padding (px) used as the top/bottom margin for scroll positioning */
export const VIEWPORT_PADDING = 8;
